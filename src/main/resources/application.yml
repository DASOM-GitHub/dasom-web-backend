server:
  port: 8080
spring:
  application:
    name: dasom-api
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB
springdoc:
  swagger-ui:
    path: /api/swagger-ui
  api-docs:
    path: /api/v1/api-docs
